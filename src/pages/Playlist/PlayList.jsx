import {NavMenu} from "../../components";

const PlayList = () => {
  return (
    <section className="app-ctn">
      <NavMenu />
      <div>
        <h2>PlayList</h2>
      </div>
    </section>
  );
};

export {PlayList};
